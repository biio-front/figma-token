{
  "dependencies": {
    "style-dictionary": "^3.7.1",
    "token-transformer": "^0.0.25"
  },
  "scripts": {
    "token:json": "./node_modules/.bin/token-transformer data/tokens.json data/output.json",
    "token": "npx style-dictionary build"
  }
}
